import Ember from 'ember';

export default Ember.Component.extend({

  actions: {
    goToPlugin(plugin) {
      // TODO: do something
      //this.get('router').transitionTo('plugins.plugin', 1);
    }
  }
});
